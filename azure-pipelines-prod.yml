# Azure DevOps — Production pipeline
# Trigger: push to main branch or manual run
# Deploys to Production environment (configure approval checks on the prod environment)

trigger:
  branches:
    include:
      - main

pr: none

variables:
  dotnetVersion: '8.0.x'
  buildConfiguration: Release
  apiProject: StudentManagementApi/StudentManagementApi.csproj
  testProject: StudentManagementApi.Tests/StudentManagementApi.Tests.csproj
  azureServiceConnection: StudentManagement-Azure

stages:
  - stage: Build
    displayName: Build and Test
    pool:
      vmImage: ubuntu-latest
    jobs:
      - job: BuildAndTest
        displayName: Restore, build, test, publish
        cancelTimeoutInMinutes: 1
        steps:
          - checkout: self
            displayName: Checkout source

          - task: UseDotNet@2
            displayName: Install .NET SDK
            condition: succeeded()
            inputs:
              packageType: sdk
              version: $(dotnetVersion)

          - task: DotNetCoreCLI@2
            displayName: Restore
            condition: succeeded()
            inputs:
              command: restore
              projects: |
                $(apiProject)
                $(testProject)

          - task: DotNetCoreCLI@2
            displayName: Build
            condition: succeeded()
            inputs:
              command: build
              projects: |
                $(apiProject)
                $(testProject)
              arguments: --configuration $(buildConfiguration) --no-restore

          - task: DotNetCoreCLI@2
            displayName: Test
            condition: succeeded()
            inputs:
              command: test
              projects: $(testProject)
              arguments: --configuration $(buildConfiguration) --no-build --logger trx
              publishTestResults: true

          - task: DotNetCoreCLI@2
            displayName: Publish API
            condition: succeeded()
            inputs:
              command: publish
              publishWebProjects: false
              projects: $(apiProject)
              arguments: --configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)/api /p:UseAppHost=false
              zipAfterPublish: false

          - task: PublishPipelineArtifact@1
            displayName: Publish API artifact
            condition: succeeded()
            inputs:
              targetPath: $(Build.ArtifactStagingDirectory)/api
              artifact: api
              publishLocation: pipeline

  - stage: DeployProd
    displayName: Deploy Production
    dependsOn: Build
    condition: succeeded('Build')
    variables:
      - group: StudentManagement-Prod
    pool:
      vmImage: ubuntu-latest
    jobs:
      - deployment: DeployProd
        displayName: Deploy database and API to Production
        environment: prod
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: self

                - template: pipelines/templates/deploy-database.yml
                  parameters:
                    azureServiceConnection: $(azureServiceConnection)

                - template: pipelines/templates/deploy-api.yml
                  parameters:
                    azureServiceConnection: $(azureServiceConnection)
                    azureWebAppName: $(azureWebAppName)
                    aspnetcoreEnvironment: Production
                    apiConnectionString: $(apiConnectionString)
                    artifactName: api
