parameters:
  - name: azureServiceConnection
    type: string
    default: 'StudentManagement-Azure'

steps:
  - task: AzureCLI@2
    displayName: Open SQL firewall for pipeline agent
    condition: succeeded()
    inputs:
      azureSubscription: ${{ parameters.azureServiceConnection }}
      scriptType: pscore
      scriptLocation: scriptPath
      scriptPath: $(Build.SourcesDirectory)/StudentManagement.Database/deploy/Open-SqlFirewallForAgent.ps1
      arguments: >-
        -SqlServer "$(sqlServer)"
        -ResourceGroup "$(sqlResourceGroup)"
        -BuildId "$(Build.BuildId)"
        -ServiceConnectionName "${{ parameters.azureServiceConnection }}"

  - task: PowerShell@2
    displayName: Install sqlcmd (Linux)
    condition: and(succeeded(), eq(variables['Agent.OS'], 'Linux'))
    inputs:
      targetType: inline
      script: |
        if (-not (Get-Command sqlcmd -ErrorAction SilentlyContinue)) {
          curl -fsSL https://packages.microsoft.com/keys/microsoft.asc | sudo gpg --dearmor -o /usr/share/keyrings/microsoft-prod.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/microsoft-prod.gpg] https://packages.microsoft.com/ubuntu/22.04/prod jammy main" | sudo tee /etc/apt/sources.list.d/mssql-release.list
          sudo apt-get update
          sudo ACCEPT_EULA=Y apt-get install -y mssql-tools18 unixodbc-dev
          echo "##vso[task.prependpath]/opt/mssql-tools18/bin"
        }

  - task: PowerShell@2
    displayName: Deploy database scripts
    condition: succeeded()
    env:
      SQL_PASSWORD: $(sqlPassword)
    inputs:
      targetType: inline
      pwsh: true
      script: |
        $ErrorActionPreference = 'Stop'
        $sqlServer = '$(sqlServer)'
        if ($sqlServer -match 'database\.windows\.net' -and '$(sqlFirewallManaged)' -ne 'true') {
          throw 'Azure SQL firewall was not opened. Check the Open SQL firewall step.'
        }
        $scriptParams = @{
          Server   = $sqlServer
          Database = '$(sqlDatabase)'
          Username = '$(sqlUsername)'
          Password = $env:SQL_PASSWORD
        }
        if ($sqlServer -match 'database\.windows\.net') {
          $scriptParams.TrustServerCertificate = $true
        }
        & '$(Build.SourcesDirectory)/StudentManagement.Database/deploy/Deploy-Database.ps1' @scriptParams

  - task: AzureCLI@2
    displayName: Close SQL firewall for pipeline agent
    condition: and(always(), eq(variables['sqlFirewallManaged'], 'true'), ne(variables['sqlFirewallRuleName'], ''))
    inputs:
      azureSubscription: ${{ parameters.azureServiceConnection }}
      scriptType: pscore
      scriptLocation: scriptPath
      scriptPath: $(Build.SourcesDirectory)/StudentManagement.Database/deploy/Close-SqlFirewallForAgent.ps1
      arguments: >-
        -ResourceGroup "$(sqlResourceGroupResolved)"
        -ServerName "$(sqlServerShortName)"
        -RuleName "$(sqlFirewallRuleName)"
